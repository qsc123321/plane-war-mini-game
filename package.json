{
  "name": "plane-war-mini-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open /dev.html",
    "build": "vite build",
    "build:direct-open": "vite build --config vite.config.ts --mode direct-open && node scripts/sync-direct-open-bundle.mjs",
    "preview": "vite preview --open /dev.html"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.10"
  }
}
